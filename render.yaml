services:
  - type: web
    name: camera-as-synth
    runtime: python
    pythonVersion: "3.10.12"
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --bind 0.0.0.0:$PORT --workers 1 --timeout 300

